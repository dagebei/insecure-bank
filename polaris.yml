version: "1"
project:
  name: insecure-bank
  branch: main
  revision:
    name: ${scm.git.commit}
    date: ${scm.git.commit.date}
capture:
  coverity:
    autoCapture: enable
analyze:
  mode: central
install:
  coverity:
    version: default
serverUrl: https://poc227.polaris.synopsys.com
